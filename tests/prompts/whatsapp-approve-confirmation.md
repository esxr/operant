You have a Chrome browser connected via my-browser MCP. WhatsApp Web is already open in Tab 1.

Steps:
1. Use browser_tabs to list tabs — Tab 1 is WhatsApp Web (web.whatsapp.com)
2. Switch to Tab 1 if not already active
3. Use browser_snapshot to see the current state
4. Find the most recent message from the Twilio number — it will be a CONFIRMATION message
5. Click on that conversation if not already open
6. Type exactly '1' in the message input box and press Enter to approve and ship
7. Use browser_snapshot to confirm the message '1' was sent
